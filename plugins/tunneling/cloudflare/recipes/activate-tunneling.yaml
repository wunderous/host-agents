contractVersion: runtime-recipe.v1
recipeId: com.opute.cloudflare.activate-tunneling
recipeVersion: 1.0.0
runtime:
  id: provider-tunneling
  servingContract: tunneling.v1
  capabilities: [tunneling]
activation:
  capability: tunneling
  servingContract: tunneling.v1
  inputBindings:
    providerId: providerId
compatibility:
  requiredTools: [get_host_info]
inputs:
  providerId:
    default: com.opute.cloudflare
    schema: {type: string, const: com.opute.cloudflare}
  activationNonce:
    default: default
    schema: {type: string, minLength: 1, maxLength: 128}
plan:
  contractVersion: host-plan.v1
  planId: com.opute.cloudflare.activate-tunneling
  generation: 1
  idempotencyKey: com.opute.cloudflare.tunneling-${vars.inputs.providerId}-${vars.inputs.activationNonce}
  nodes:
    - id: host
      action:
        tool: get_host_info
