contractVersion: host-recipe.v1
recipeId: com.opute.tailscale.overlay-mesh
recipeVersion: 1.1.0
execution:
  coordinator: host-agent
  mode: local
compatibility:
  requiredTools:
    - get_host_info
    - opute.capability.mesh-membership.enroll
    - opute.capability.mesh-membership.status
    - opute.capability.private-mesh.ensure
    - opute.capability.private-mesh.probe
inputs:
  targetUri:
    required: true
    schema: {type: string, minLength: 1}
  peerTargetUri:
    required: true
    schema: {type: string, minLength: 1}
  peerMeshIp:
    required: true
    schema: {type: string, format: ipv4}
  name:
    required: true
    schema: {type: string, minLength: 1}
plan:
  contractVersion: host-plan.v1
  planId: com.opute.tailscale.overlay-mesh
  generation: 1
  idempotencyKey: com.opute.tailscale.overlay-mesh-${vars.inputs.targetUri}-v3
  nodes:
    - id: agent
      action:
        tool: get_host_info
    - id: enroll
      dependsOn: [agent]
      action:
        tool: opute.capability.mesh-membership.enroll
        args:
          hostAgentId: ${nodes.agent.output.agent.agentId}
          targetUri: ${vars.inputs.targetUri}
          name: ${vars.inputs.name}
          credentialKind: auth-key
      validate:
        tool: opute.capability.mesh-membership.status
        args:
          hostAgentId: ${nodes.agent.output.agent.agentId}
          targetUri: ${vars.inputs.targetUri}
        assert:
          - {path: /ready, op: eq, value: true}
        pollIntervalMs: 2000
        timeoutMs: 180000
    - id: mesh
      dependsOn: [enroll]
      action:
        tool: opute.capability.private-mesh.ensure
        args:
          hostAgentId: ${nodes.agent.output.agent.agentId}
          targetUri: ${vars.inputs.targetUri}
          membershipRef: ${nodes.enroll.output.membershipRef}
          peerTargetUri: ${vars.inputs.peerTargetUri}
          peerMeshIp: ${vars.inputs.peerMeshIp}
          credentialKind: auth-key
      validate:
        tool: opute.capability.private-mesh.probe
        args:
          hostAgentId: ${nodes.agent.output.agent.agentId}
          targetUri: ${vars.inputs.targetUri}
          peerMeshIp: ${vars.inputs.peerMeshIp}
        assert:
          - {path: /ready, op: eq, value: true}
        pollIntervalMs: 2000
        timeoutMs: 180000
    - id: probe
      dependsOn: [mesh]
      action:
        tool: opute.capability.private-mesh.probe
        args:
          hostAgentId: ${nodes.agent.output.agent.agentId}
          targetUri: ${vars.inputs.targetUri}
          peerMeshIp: ${vars.inputs.peerMeshIp}
