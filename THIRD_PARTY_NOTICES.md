# Third-party notices

The Go module dependencies and their licenses are recorded by the upstream
module metadata and the repository's `go.sum`. Release automation should
refresh this file from the dependency graph before a public release. The npm
launcher uses only Node.js standard-library modules and has no npm runtime
dependencies.
